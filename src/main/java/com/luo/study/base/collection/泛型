集合中的元素，可以是任意类型的对象（对象的引用）
如果把某个对象放入集合，则会忽略他的类型，而把他当做Object处理。
泛型则是规定了某个集合只可以存放特定类型的对象，会在编译期间进行类型检查，可以直接按指定类型获取集合元素

泛型集合中除了可以存放泛型类型的对象引用，还可以存放泛型对象的子类型的对象引用
泛型集合中的限定类型不能是使用基本数据类型，可以通过使用包装类限定允许存入的基本数据类型

